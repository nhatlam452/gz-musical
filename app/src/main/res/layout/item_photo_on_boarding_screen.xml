<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ImageView
        android:id="@+id/imgItemOnBoarding"
        android:padding="20dp"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@id/tvSloganItemOnBoarding"
        android:scaleType="centerCrop"
         />
    <TextView
        android:id="@+id/tvSloganItemOnBoarding"
        android:textColor="@color/black"
        android:layout_alignParentBottom="true"
        android:layout_width="300dp"
        android:layout_height="wrap_content"
        android:textSize="14dp"
        android:layout_centerInParent="true"
        android:textAlignment="center"
        android:textStyle="italic"
        android:text="“To inspire and nurture the human spirit–one person, one cup and one neighborhood at a time.”"
        />
</RelativeLayout>